# Backend HTTP, SSE, and webhook API QA specification

Authoritative route census for the local Helmryth backend as of August 30, 2026, derived from `server/index.ts`, `server/webhook-ingress.ts`, and the current server test corpus.

## Exact route count

| Surface | Exact method/path pairs |
|---|---:|
| Main server internal routes | 13 |
| Main server public routes | 123 |
| Webhook sidecar routes | 3 |
| Total documented in this file | 139 |

This chapter enumerates every current backend route exactly once. It restores the routes and verbs that earlier drafts missed or misstated, including the main health probe, the per-instance CLI override, the card patch routes, the connector-card status read, the skills and checkpoint surfaces, and the Box, VPS, and Local VM lifecycle endpoints.

## Global gates and invariants

- Main-server routes accept only the exact `127.0.0.1:<port>` `Host` authority. Every other authority, including the loopback aliases `localhost` and `[::1]`, is rejected with `403 forbidden: exact loopback authority required`.
- Main-server routes reject disallowed `Origin` with `403 forbidden: cross-origin request`.
- The main-server event stream route must emit a hello frame first, honor `Last-Event-ID` over a stale `?since=` cursor, and return `resumed: false` when the replay window cannot safely satisfy the request.
- `/api/internal/*` routes additionally require the boot-token bearer checked by `authorizedComms`; failures return `401 unauthorized`.
- JSON-only mutation gates return `415 content-type must be application/json` before any side effect.
- The webhook sidecar is separate from the main API. Its health route is anonymous. Its hook routes are POST-only capability endpoints with a `256 KiB` body cap and `401` on invalid secret.

## Coverage legend

| Mark | Meaning |
|---|---|
| Direct | Current route-level integration or end-to-end test located. |
| Service | Covered mainly through lower-level service tests, worker tests, or adjacent route tests; not a full route assertion. |
| Gap | No strong current route-level assertion located. |

## Route matrix

| ID | Exact routes | Contract and primary assertions | Current evidence | Coverage |
|---|---|---|---|---|
| `DISC-01` | `GET /api/health`, `GET /api/events`, `GET /api/decisions` | Health returns `{app:"helmryth",pid,static}`; events is `text/event-stream` with hello, replay, keepalive, and stale-cursor hydrate fallback; decisions pages newest-last with positive-whole-number `limit` validation. | `server/index.test.ts`, `server/testing/sse.test.ts`, `server/decision-log-wiring.test.ts` | Direct |
| `DISC-02` | `GET /api/instances`, `GET /api/cli-candidates`, `POST /api/cli-test`, `PATCH /api/instances/:id` | Instance listing rescans PATH before describing availability; CLI candidate listing returns absolute-path candidates; CLI probe is JSON-only and rejects empty or newline-bearing paths; instance patch persists exact CLI override, reloads providers, and rejects concurrent provider config writes. | `server/index.test.ts`, `server/mcp-server.test.ts` | Service |
| `DISC-03` | `GET /api/config`, `PUT /api/config`, `PATCH /api/config` | Config read never echoes secrets; writes validate patch shape, reject empty writes, serialize through `providerConfigBusy`, validate Box, TTS, and Composio credentials before save, support external secret storage, and reload providers only for keys that change fleet behavior. | `server/index.test.ts`, `server/vps-routing.test.ts`, `server/comms.test.ts` | Direct |
| `DISC-04` | `GET /api/tts/voices`, `POST /api/tts/prepare`, `POST /api/tts/speak` | Voices list degrades to `200 {voices:[],error}` on provider failure; prepare returns `ready` plus utterance segmentation; speak requires non-empty text, enforces a 500-character cap, returns audio bytes on success, `409` for not-configured voice, and `502` for provider failure. | `src/lib/tts/index.test.ts`, `server/index.ts` route logic | Service |
| `OPS-01` | `GET /api/team-map`, `GET /api/section-context`, `PUT /api/section-context` | Team map is a privacy-safe projection without transcript text or delegated prompt leakage; section context requires a `section` query even for General, validates 60-character section keys and byte caps, and persists only user-owned brief text. | `server/index.test.ts`, `server/delegations.test.ts` | Direct |
| `OPS-02` | `GET /api/routines`, `POST /api/routines`, `POST /api/routines/:id/run`, `PATCH /api/routines/:id`, `DELETE /api/routines/:id`, `POST /api/routine-runs/:id/cancel`, `POST /api/routine-runs/:id/seen` | Routine list supports bounded `from` and `to` run filtering; create returns `201`; manual run returns `201` or `404`; patch and delete target one cadence only; run cancel and seen target one active run only and return `404` for unknown active run ids. | `server/index.test.ts`, `server/routines.test.ts` | Direct |
| `OPS-03` | `GET /api/webhooks`, `POST /api/webhooks`, `POST /api/webhooks/:id/rotate`, `POST /api/webhooks/:id/test`, `PATCH /api/webhooks/:id`, `DELETE /api/webhooks/:id` | Webhook list exposes webhook metadata, attempts, and ingress status but never secrets; create and rotate return a one-time credential bundle; test returns `202` or `404`; patch preserves secret while validating fields; delete removes one webhook and future deliveries fail auth. | `server/webhooks.test.ts`, `server/index.test.ts`, `server/unattended.test.ts` | Service |
| `OPS-04` | `GET /health`, `POST /hooks/:endpointId`, `POST /hooks/:endpointId/:secret` | Sidecar health returns `{app:"helmryth-webhooks",ready:true}` only; hook routes accept JSON, `+json`, form-encoded, or raw text according to content type; invalid secret returns `401` before payload parse; malformed JSON returns `400`; oversized body returns `413`; duplicate delivery ids are idempotent; verification webhooks capture without queueing. | `server/webhook-ingress.test.ts`, `server/webhooks.test.ts` | Direct |
| `LIB-01` | `GET /api/team-library/catalog`, `GET /api/team-library/teams/:slug`, `POST /api/team-library/github` | Catalog and team reads are fetch-through views that return `502` or `404` without partial state; GitHub import requires a non-empty URL and normalizes GitHub team sources without granting runtime authority. | `server/index.ts` route logic, team-library tests in repo | Service |
| `LIB-02` | `GET /api/teams/scout`, `GET /api/teams/scout/directory` | Scout reads only a validated local folder, never creates records, returns `400` for invalid or missing folder, and degrades the optional community directory lookup to an empty list rather than a failing primary scout. | `server/index.ts` route logic, project scout tests in repo | Service |
| `LIB-03` | `POST /api/teams/export`, `POST /api/teams/import`, `GET /api/teams/imports`, `POST /api/teams/imports/:transactionId/undo` | Team export strips runtime authority, hidden bots, live transcripts, `alwaysAllow`, model selection, and thread ids; import supports `add`, `replace`, and `project`, validates optional `cwd`, creates fresh bot ids only, archives old visible bots only after a full replace succeeds, and rolls back created groups, routines, and bots on failure; the import history list and undo route expose committed import receipts for recovery. | `server/index.test.ts`, `server/package-export.test.ts`, `server/team-manifest.test.ts` | Direct |
| `THREAD-01` | `GET /api/threads/:threadId/messages`, `GET /api/threads/:threadId/messages/:messageId/image`, `GET /api/threads/:threadId/events`, `GET /api/threads/:threadId/export`, `POST /api/threads/:threadId/respond`, `POST /api/threads/:threadId/messages/:messageId/reactions` | Message paging rejects invalid limits and conflicting cursor modes, and 404s unknown threads or cursors; message image fetch refuses unknown conversations before materializing message state and serves immutable bytes only for screen messages; thread events pages runtime/native logs with positive `limit`; export supports only `markdown` or `json`; thread-scoped approval response resolves stranded room approvals; reactions toggle on one message only. | `server/index.test.ts`, `server/decision-log-wiring.test.ts`, `server/mcp-server.test.ts` | Direct except reactions Gap |
| `THREAD-02` | `POST /api/attachments`, `GET /api/attachments/:name` | Attachment upload is raw-byte, image-MIME only, and streaming-size-limited; read path is filename-locked to the attachments directory, serves `nosniff`, and returns immutable bytes for existing attachments only. | `server/index.test.ts`, avatar and attachment tests in repo | Direct |
| `THREAD-03` | `GET /api/search` | Search clamps `limit` to `1..100`, optionally scopes to one existing thread, drops deleted-conversation hits, and annotates whether a hit is on the active branch. | `server/index.test.ts`, `server/mcp-server.test.ts`, `server/message-db.test.ts` | Direct |
| `BOT-01` | `GET /api/bots`, `POST /api/bots`, `PATCH /api/bots/:id`, `DELETE /api/bots/:id` | Bot list returns bots, groups, and `computerControl`; create validates profile and optional model selection and returns `201`; patch validates every mutable field, enforces local-auto acknowledgement, per-field type checks, pinned-message id rules, browser profile existence, and busy backend restrictions; delete interrupts active work, disables routines and webhooks, cancels approvals and delegations, enforces Local VM safety, and cleans thread artifact files. | `server/index.test.ts`, `server/branching.test.ts`, `server/vps-routing.test.ts`, `server/steer-e2e.test.ts` | Direct |
| `BOT-02` | `PATCH /api/bots/:id/profile`, `POST /api/bots/:id/avatar/generate` | Profile patch is the narrow persona-safe boundary and requires an existing stored attachment for `avatarUrl`; avatar generation validates prompt length, snapshots existing avatar state before awaiting image generation, writes a stored image, and returns `409` if avatar state changed mid-flight. | `server/bot-profile.test.ts`, `server/avatar-image.test.ts` | Direct |
| `BOT-03` | `POST /api/bots/:id/read`, `POST /api/bots/:id/always-allow`, `PATCH /api/bots/:id/cards/:cardId` | Read clears unread and broadcasts; always-allow requires a pending card whose `allowKey` matches and dedupes the stored grant list up to 200 entries; card patch persists answered and dismissed state only for an existing card on that bot's main thread. | `server/index.test.ts`, `server/auto-approve.test.ts`, `server/decision-log-wiring.test.ts` | Direct except always-allow Gap |
| `BOT-04` | `POST /api/bots/:id/messages`, `DELETE /api/bots/:id/queue/:queueId`, `POST /api/bots/:id/messages/:messageId/edit`, `POST /api/bots/:id/active-branch`, `POST /api/bots/:id/respond`, `POST /api/bots/:id/interrupt` | Message send validates text, task ownership, reply target, and optional `sendId`; duplicate `sendId` returns canonical receipt while conflicting `sendId` returns `409`; edit branches only a user text message and reruns from that branch; active-branch switches the visible leaf only when idle; bot-scoped respond resolves routine and peer-approval intercepts before provider fallback; interrupt safely cancels routine, room, or direct work depending on current ownership. | `server/index.test.ts`, `server/branching.test.ts`, `server/steer-e2e.test.ts`, `server/steer-queue.test.ts`, `server/comms.test.ts` | Direct |
| `BOT-05` | `POST /api/bots/:id/tasks`, `POST /api/bots/:id/tasks/:taskId`, `PATCH /api/bots/:id/tasks/:taskId`, `DELETE /api/bots/:id/tasks/:taskId` | Bot task create, switch, rename, and delete are atomic and fail closed when the bot is busy; delete refuses the last run and refuses the active run; slim switch responses omit transcript state when `?messages=0` is used. | `server/index.test.ts`, `server/mcp-server.test.ts` | Direct |
| `BOT-06` | `GET /api/bots/:id/skills`, `POST /api/bots/:id/skills`, `GET /api/bots/:id/skills/:slug`, `PATCH /api/bots/:id/skills/:slug`, `DELETE /api/bots/:id/skills/:slug` | Skills list is bot-scoped; install accepts one source string and returns installed skill entries plus per-skill errors; read returns raw `SKILL.md` text or `404`; patch toggles `enabled`; delete removes one installed skill. | `server/index.ts` route logic | Gap |
| `BOT-07` | `GET /api/bots/:id/memory`, `PUT /api/bots/:id/memory`, `GET /api/bots/:id/memory/topics/:topic` | Memory read returns `MEMORY.md`, truncation state, and topic list without creating a workspace; write accepts only string `text` within byte cap; topic read decodes before validating and rejects traversal-shaped or malformed encoded names. | `server/index.test.ts` | Direct |
| `BOT-08` | `GET /api/bots/:id/checkpoints`, `POST /api/bots/:id/checkpoints/restore` | Checkpoint list requires a `cwd` query, reports `enabled`, and returns only that bot's checkpoint history; restore validates absolute `cwd` and full 40-character hash, refuses when the bot is busy or already restoring, and uses a lease to prevent concurrent restore/write races. | `server/index.ts` route logic, checkpoint tests referenced elsewhere in repo | Direct |
| `BOT-09` | `POST /api/bots/:id/secret-cards/:messageId/provided`, `POST /api/bots/:id/secret-cards/:messageId/resume`, `POST /api/bots/:id/secret-cards/:messageId/dismiss` | Secret-card routes never accept the credential value; `provided` requires that the target credential is already configured and the request was not dismissed; `resume` replays only a ready `provided` or `dismissed` outcome; `dismiss` resumes with dismissal only when the value was not already provided. | `server/index.test.ts` | Direct for dismiss, Gap for provided and resume happy path |
| `BOT-10` | `POST /api/bots/:id/connector-cards/:messageId/authorize`, `GET /api/bots/:id/connector-cards/:messageId/status`, `POST /api/bots/:id/connector-cards/:messageId/resume`, `POST /api/bots/:id/connector-cards/:messageId/dismiss` | Connector-card routes are bot-and-thread bound; authorize marks the card `authorizing`, returns the provider auth payload, and truncates stored error text on failure; status is the only GET in this family and updates the card from connection status; resume succeeds only when every app tied to the `resumeKey` is connected; dismiss marks one card dismissed without resuming the paused task. | `server/index.ts` route logic, connected-app service tests | Direct for status/dismiss, Gap for authorize/resume |
| `GROUP-01` | `POST /api/groups`, `PATCH /api/groups/:id`, `DELETE /api/groups/:id`, `POST /api/groups/:id/read` | Group create validates roster, optional name and section, and optional setup bundle; patch validates name, bulletin, unread, responder, cwd, pin, and section, blocks structural edits while the room is working or waiting, and refuses member changes on DM groups; delete removes group state and thread artifacts but refuses while working; read clears unread and broadcasts. | `server/index.test.ts`, `server/mcp-server.test.ts` | Direct except group read Gap |
| `GROUP-02` | `PATCH /api/groups/:id/setup` | Setup is PATCH, not POST. It accepts only `complete` or `skip`, refuses DMs, refuses any change after a first room message, validates cwd, bulletin, and default responder on complete, and is idempotent once completed or skipped. | `server/index.test.ts` | Direct |
| `GROUP-03` | `POST /api/groups/:id/messages`, `POST /api/groups/:id/interrupt` | Group send validates body, text, optional task thread, reply target, and `sendId`; it rejects stale task ownership with `409` rather than writing into a new task, and queues or steers with the same idempotency guard pattern as bot send; group interrupt cancels queued responder work first, interrupts the current busy member if one exists, and closes open approvals. | `server/index.test.ts`, `server/mcp-server.test.ts` | Direct |
| `GROUP-04` | `POST /api/groups/:id/tasks`, `POST /api/groups/:id/tasks/:taskId`, `PATCH /api/groups/:id/tasks/:taskId`, `DELETE /api/groups/:id/tasks/:taskId` | Group task create, switch, rename, and delete exist only for non-DM crews, are blocked while the room is working or waiting on a pending card, and delete refuses the last remaining task while cleaning `lastReply` state for the removed task. | `server/index.test.ts`, `server/mcp-server.test.ts` | Direct |
| `LOCAL-01` | `GET /api/local-computer`, `POST /api/local-computer/pull`, `POST /api/local-computer/run`, `POST /api/local-computer/start`, `POST /api/local-computer/stop`, `POST /api/local-computer/remove`, `POST /api/local-computer/screenshot`, `POST /api/local-computer/interrupt` | Shared Local VM status is read-only; lifecycle routes are JSON-only and serialize through image, mode, and lifecycle busy flags; run is forbidden in per-bot mode; stop and remove refuse while a bot owns the shared VM; screenshot touches idle timeout; interrupt is idempotent and best-effort across all bots using local computer mode. | `server/index.test.ts`, `server/local-computer.test.ts`, `server/container-computer.test.ts` | Direct for interrupt, Service for most lifecycle routes |
| `LOCAL-02` | `GET /api/bots/:id/local-computer`, `POST /api/bots/:id/local-computer/run`, `POST /api/bots/:id/local-computer/stop`, `POST /api/bots/:id/local-computer/remove`, `POST /api/bots/:id/local-computer/screenshot` | Per-bot Local VM status is bot-scoped; lifecycle is JSON-only, refuses shared-mode targets, enforces one-target-at-a-time lifecycle plus global capacity and owner checks, and updates idle timers on run or screenshot. | `server/index.ts` route logic, Local VM service tests in repo | Service |
| `REMOTE-01` | `GET /api/bots/:id/computer`, `GET /api/bots/:id/computer/control`, `POST /api/bots/:id/computer/control`, `POST /api/bots/:id/computer/viewer-close` | Computer status chooses exactly one backend, Box or VPS; control GET returns the current snapshot; control POST is JSON-only and supports `take`, `release`, and `dismiss-help`, with optional lease-aware variants; viewer-close is JSON-only and is a Box no-op returning `{closed:false}` while VPS closes the desktop tunnel. | `server/index.test.ts`, `server/vps-routing.test.ts` | Direct for control, Service for viewer-close |
| `REMOTE-02` | `POST /api/bots/:id/computer/provision`, `POST /api/bots/:id/computer/join`, `POST /api/bots/:id/computer/sleep`, `POST /api/bots/:id/computer/exec`, `POST /api/bots/:id/computer/screenshot`, `POST /api/bots/:id/computer/remove` | These routes are JSON-only and backend-specific: VPS forbids exec, gates provision on cloud-destination intent or auto-start, blocks sleep and remove while the VPS is in active use, and refuses join from the companion path; Box forbids remove, supports exec, and otherwise uses Box-specific lifecycle calls. | `server/index.ts` route logic, `server/vps-routing.test.ts`, Box and VPS service tests in repo | Service |
| `CAP-01` | `GET /api/connectors/catalog`, `GET /api/connectors/connected`, `GET /api/connectors` | Catalog returns toolkit cards plus source and configured mode; connected returns a degraded configured=false shape when the credential store is unreadable or the service is unconfigured; generic connectors status optionally scopes by `services=<csv>` and otherwise uses curated slugs. | `server/index.ts` route logic, connected-app tests in repo | Service |
| `CAP-02` | `POST /api/connectors/:slug/authorize`, `DELETE /api/connectors/:slug/accounts/:accountId`, `DELETE /api/connectors/:slug` | Top-level connector authorize proxies the provider connect flow for one slug with optional alias; account delete removes one concrete account id; service delete removes one service selection. Earlier slug-scoped read and create assumptions are not part of the current server contract. | `server/index.ts` route logic | Gap |
| `INT-01` | `GET /api/internal/agents` | Internal roster lookup requires boot-token auth and a valid sender bot in `self`; returns only same-section, non-hidden peer metadata and no transcript content. | `server/comms.test.ts`, `server/drivers/agents-proxy.test.ts` | Direct |
| `INT-02` | `GET /api/internal/routines`, `POST /api/internal/routine-requests` | Internal routine list requires source bot and owned source thread, returns routine projections plus latest durable run state, and redacts legacy secret-bearing text; routine requests validate the proposal envelope, refuse foreign threads, and append an approval card plus decision-log row instead of mutating a routine immediately. | `server/index.test.ts`, `server/drivers/agents-proxy.test.ts` | Direct |
| `INT-03` | `POST /api/internal/ask-bot`, `POST /api/internal/delegate-bot`, `GET /api/internal/delegations/:taskId` | Ask-bot and delegate-bot require valid sender, target, thread ownership, and same-section routing; ask-bot can return peer reply, denial, or busy signal; delegate-bot returns a queued receipt and later long-polls through the delegation status route; long-poll checks workstream ownership before exposing the receipt. | `server/comms.test.ts`, `server/delegations.test.ts`, `server/drivers/agents-proxy.test.ts` | Direct |
| `INT-04` | `POST /api/internal/create-bot`, `POST /api/internal/request-credential` | Internal create-bot requires a chief-of-staff source and enforces workspace cap, duplicate-name checks, and safe default flags; request-credential validates supported credential ids, reuses existing unresolved cards, and never accepts the secret value itself. | `server/comms.test.ts`, `server/index.test.ts` | Direct |
| `INT-05` | `POST /api/internal/connectors/mcp`, `POST /api/internal/connectors/request` | Connectors MCP relay proxies status, content type, and `mcp-session-id` from the upstream MCP session; connector request validates `resumeKey`, bot and thread ownership, slug count and shape, and connected-app availability before appending one card per slug or reusing an existing one. | `server/composio.test.ts`, `server/drivers/acp/acp.test.ts`, `server/drivers/codex.test.ts` | Direct |
| `INT-06` | `GET /api/internal/computer-control`, `POST /api/internal/computer-control`, `DELETE /api/internal/computer-control` | Internal computer-control is the bot-side control lease API. GET exposes only `held` and `helpOpen`; POST requests help and returns a resumable `requestId` plus notification side effect; DELETE expires one help request by id without exposing or changing unrelated leases. | `server/index.test.ts`, `server/computer-proxy.test.ts` | Direct for auth and basic contract, Service for full help-lease lifecycle |

## Highest-signal route gaps after the re-audit

| Family ID | Why it is still weak |
|---|---|
| `BOT-06` | Skills routes are live in `server/index.ts` but I did not find a strong route-level integration suite that exercises install, read, toggle, and delete through HTTP. |
| `BOT-09` | Secret-card dismiss is covered, but provided and resume happy paths still lack a strong direct route proof in the current server suite. |
| `BOT-10` | Connector-card authorize and resume routes are present but currently under-proven at the route layer. |
| `GROUP-01` | Group unread-clear exists, but I did not find a crisp dedicated route assertion for it. |
| `THREAD-01` | Reaction toggling is implemented on the route, but I did not find a strong direct HTTP assertion that covers add, remove, and actor attribution. |
| `LOCAL-01`, `LOCAL-02`, `REMOTE-02` | Most VM and remote-computer lifecycle routes are protected by careful route logic and service tests, but many exact HTTP branches still rely more on lower-level coverage than on direct route assertions. |
| `CAP-02` | Top-level connector authorize and delete flows are present, but the route-level coverage signal is weaker than the bot-scoped card and MCP-relay paths. |

## Verification notes

- The exact local backend census is `139` routes: `13` internal main-server, `123` public main-server, `3` webhook-sidecar.
- This file keeps the inventory matrix as the single authoritative route list for this chapter.
- The main corrections from the prior draft are the health payload shape, the card patch semantics, the connector-card status read, the group-setup patch verb, the missing skills and checkpoint surfaces, the per-instance CLI override, and the removal of previously invented slug-scoped connector and bot/group read routes.
