---
title: Permissions and secrets
description: How gates, operating-system grants, and provider credentials are handled.
icon: KeyRound
---

## Gates

Supported providers emit gate requests and questions through the local harness. Helmryth displays these inline and records the outcome. Cancellation closes outstanding requests so stale gates cannot be answered after a run has ended.

“Always allow” is narrow: it is tied to a server-issued key and a pending request rather than granting arbitrary execution from a paired device.

## Operating-system permissions

Screen Recording and Accessibility permissions are controlled by the operating system. The desktop process owns local CUA lifecycle so grants are attributed to Helmryth.

## Secret handling

- Do not paste API keys into workstreams.
- Packaged builds use write-only System controls and operating-system-backed encryption where supported.
- Secret values are scrubbed from public configuration responses and mobile APIs.
- Per-provider environment injection prevents unrelated provider processes from inheriting credentials they do not use.
- OAuth provider tokens for capabilities stay with Composio.

## Shared machines

Helmryth assumes the logged-in operating-system user is the workspace owner. On a shared machine, other administrators may be able to read application data or inspect processes. Use a dedicated OS account for stronger separation.
